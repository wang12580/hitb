defmodule Server.ShareServiceTest do
  use Hitb.DataCase, async: false
  alias Server.ShareService


  # test "test share" do
  #   assert ShareService.share("cdh", "", "sss", "") == :ok
  # end
  #
  # test "test get_share" do
  #   assert %{key: "edit", last: "-", val: 0} in ShareService.get_share()
  # end

  # test "test insert!" do
  #   {:ok, record} = ShareService.create_record(@record)
  #   assert ShareService.get_record!(record.id).mode == @record.mode
  # end


end
